import { DomainEvent } from '@stratix/primitives';
import { <%= entityName %>Id } from '../entities/<%= entityName %>.js';

export class <%= eventName %> implements DomainEvent {
  public readonly occurredAt: Date;

  constructor(
    public readonly <%= entityNameCamel %>Id: <%= entityName %>Id<% if (props.length > 0) { %>,<% } %>
<% props.forEach((prop, index) => { %>
    public readonly <%= prop.name %>: <%= prop.type %><%= index < props.length - 1 ? ',' : '' %>
<% }); %>
  ) {
    this.occurredAt = new Date();
  }
}
