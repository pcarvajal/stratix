import { Repository } from '@stratix/abstractions';
import { <%= entityName %>, <%= entityName %>Id } from '../entities/<%= entityName %>.js';

export interface <%= repoName %> extends Repository<<%= entityName %>, <%= entityName %>Id> {
  // Base methods from Repository interface:
  // - findById(id: <%= entityName %>Id): Promise<<%= entityName %> | null>
  // - save(entity: <%= entityName %>): Promise<void>
  // - delete(id: <%= entityName %>Id): Promise<void>

  // Add custom query methods here
  findAll(): Promise<<%= entityName %>[]>;
}
